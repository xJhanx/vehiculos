const formProveedorRegister = document.getElementById('formProveedorRegister');
formProveedorRegister.addEventListener('submit', ajaxFormRegisterProveedor);

const formProveedorUpdate = document.getElementById('formProveedorUpdate');
formProveedorUpdate.addEventListener('submit', ajaxFormUpdateProveedor);